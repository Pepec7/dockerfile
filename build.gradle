plugins {
  id "org.sonarqube" version "3.3"
}

sonarqube {
  properties {
    property "sonar.projectKey", "Pepec7_dockerfile"
    property "sonar.organization", "pepec7"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
